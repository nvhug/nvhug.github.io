# Replace notez.example.com with your real domain before deploying.
# Caddy requests and renews the Let's Encrypt certificate automatically.
{
	email your-email@example.com
}

notez.example.com {
	encode zstd gzip
	reverse_proxy web:3000
}
